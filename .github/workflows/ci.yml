name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test-and-build:
    name: Test & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: fluid-tester
        run: npm ci

      - name: Lint
        working-directory: fluid-tester
        run: npm run lint

      - name: Test
        working-directory: fluid-tester
        run: npm test

      - name: Build
        working-directory: fluid-tester
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fluid-tester-dist
          path: fluid-tester/dist
          retention-days: 14
